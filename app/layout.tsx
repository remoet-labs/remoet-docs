import { Footer, Layout, Navbar } from 'nextra-theme-docs'
import { Head } from 'nextra/components'
import { getPageMap } from 'nextra/page-map'
import 'nextra-theme-docs/style.css'
import type { Metadata } from 'next'

const SITE_URL = 'https://docs.remoet.dev'
const DEFAULT_TITLE = 'Remoet developer documentation'
const DEFAULT_DESCRIPTION =
  'Connect an AI agent to Remoet over MCP, or fetch your own profile over the REST API. Free, no plans.'

// The OG image is generated by app/opengraph-image.tsx. Next injects the
// og:image / twitter:image tags into THIS metadata, so any segment that
// defines its own openGraph has to re-declare images or the tags are lost.
// See app/[[...mdxPath]]/page.tsx, which is where that bit us.
export const metadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: {
    template: '%s | Remoet docs',
    default: DEFAULT_TITLE,
  },
  description: DEFAULT_DESCRIPTION,
  openGraph: {
    title: DEFAULT_TITLE,
    description: DEFAULT_DESCRIPTION,
    url: SITE_URL,
    siteName: 'remoet.dev docs',
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    site: '@remoet_dev',
    title: DEFAULT_TITLE,
    description: DEFAULT_DESCRIPTION,
  },
  alternates: {
    canonical: SITE_URL,
  },
  robots: {
    index: true,
    follow: true,
  },
}

const websiteJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'Remoet developer documentation',
  url: SITE_URL,
  publisher: {
    '@type': 'Organization',
    name: 'Remoet',
    url: 'https://www.remoet.dev',
  },
}

export default async function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const navbar = (
    <Navbar
      logo={<span>remoet.dev</span>}
      projectLink="https://github.com/remoet-labs/remoet-docs"
      chatLink="https://discord.gg/yxJA6cBs8U"
    />
  )
  const pageMap = await getPageMap()
  return (
    <html lang="en" dir="ltr" suppressHydrationWarning>
      <Head />
      <body>
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(websiteJsonLd) }}
        />
        <Layout
          navbar={navbar}
          footer={<Footer>remoet.dev © 2026</Footer>}
          docsRepositoryBase="https://github.com/remoet-labs/remoet-docs/blob/main"
          pageMap={pageMap}
        >
          {children}
        </Layout>
      </body>
    </html>
  )
}
